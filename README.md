# CI Jenkins Multi-Build Demo

A small Java application designed for the DevOps Jenkins CI practical.

The same application can be built with:
- Maven
- Apache Ant
- Gradle

Application:
- `App.add(2, 3)` returns `5`
- `AppTest` is a simple automated test that reports PASS/FAIL.
